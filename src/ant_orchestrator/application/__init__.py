"""Application layer: use-case services, outbound ports and result models.

Depends on ``core`` and ``config`` only; it orchestrates infrastructure adapters
through outbound ports and never imports concrete workspace/persistence modules.
"""
