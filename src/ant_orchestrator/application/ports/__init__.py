"""Outbound ports the application drives (workspace provisioning, database).

State enums declared here are part of the port *contract*; adapters in the
workspace/persistence layers compute and return them (PHASE_1_PLAN §9/§12.2, D29/D30).
"""
