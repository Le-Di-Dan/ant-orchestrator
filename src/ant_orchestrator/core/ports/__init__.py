"""Inner persistence/boundary ports (Protocols) for the domain.

Depends only on ``core/domain`` and stdlib typing. Concrete adapters live in the
infrastructure layers (persistence, workspace, cli composition root).
"""
