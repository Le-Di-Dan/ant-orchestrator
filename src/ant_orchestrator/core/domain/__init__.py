"""Pure domain layer: entities, records, value objects, enums and domain errors.

This package depends only on the standard library and ``ant_orchestrator.errors``.
It must never import infrastructure (sqlite3, yaml, typer) or provider SDKs.
"""
